import Link from "next/link";
import { AddToProgramForm } from "@/src/components/exercise/add-to-program-form";
import { PrimaryButton } from "@/src/components/ui/primary-button";
type ProgramOption = { id: string; name: string; days: Array<{ id: string; dayIndex: number; title: string }> };
export function ExerciseAddToProgram({ exerciseId, programs, action }: { exerciseId?: string; programs: ProgramOption[]; action: (formData: FormData) => void | Promise<void> }) { return <section className="exercisePanel addToProgram" aria-labelledby="exercise-add-title"><div className="sectionHeading"><span>Programme</span><h2 id="exercise-add-title">Ajouter à mon programme</h2></div>{exerciseId && programs.length ? <AddToProgramForm exerciseId={exerciseId} programs={programs} action={action} /> : exerciseId ? <div className="addEmpty"><p>Créez d&apos;abord un programme pour y ajouter cet exercice.</p><Link href="/programs"><PrimaryButton>Créer un programme</PrimaryButton></Link></div> : <p className="addEmpty">L&apos;ajout réel sera disponible dès que cet exercice pilote sera intégré au catalogue validé. Aucune entrée catalogue n&apos;a été créée automatiquement.</p>}</section>; }
