export function ExerciseHero({ canonicalName, displayNameFr, badges, showBranding = true }: { canonicalName: string; displayNameFr: string; badges: readonly string[]; showBranding?: boolean }) {
  return <header className="exerciseHero">{showBranding ? <div className="exerciseBrand"><strong>TRAKNIO</strong><span>Plus qu&apos;un suivi. Une progression.</span></div> : null}<div className="exerciseHeroCopy"><p>Fiche technique</p><h1>{canonicalName}</h1><p className="exerciseSubtitle">{displayNameFr}</p><div className="exerciseBadges">{badges.map((badge, index) => <span className={index === 0 ? "isPrimary" : undefined} key={badge}>{badge}</span>)}</div></div>{showBranding ? <p className="exerciseMotto">Entraînez<br />Progressez<br />Vivez mieux</p> : null}</header>;
}
