# Android entry points and library reflection rules are supplied by AGP and
# dependency consumer rules. Watch payloads use explicit JSONObject parsing;
# do not keep all application classes, which would disable obfuscation.
